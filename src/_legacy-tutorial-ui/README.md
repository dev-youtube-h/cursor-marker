# 旧チュートリアル UI (ビルド対象外)

カーソルマーカーアプリを作る前にあった、サイドバー + ダッシュボードの
チュートリアル用 UI 一式です。参照用に残してあるだけで、
tsconfig / vite / eslint のいずれの対象にも含まれていません
(`App.tsx` から `ViewId` を import しているので、そのままでは型エラーになります)。

不要であればこのフォルダごと削除して構いません。
